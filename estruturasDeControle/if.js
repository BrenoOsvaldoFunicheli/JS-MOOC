// É possível utilizar os vários elementos que podem ser boolean

function GoodNews(note) {
    if (note > 7) {
        return note
    }
}

console.log(GoodNews(8))


// Please note the same value in js represents booleand value
// so you don't need put !!

function isTrue()